id: partyvoice23922.dscawayswitch
version: 1
status: proposed
name: dscawayswitch
attributes:
  switch:
    schema:
      type: object
      properties:
        value:
          type: string
      additionalProperties: false
      required:
        - value
    setter: setSwitch
    enumCommands: []
commands:
  setSwitch:
    name: setSwitch
    arguments:
      - name: value
        optional: false
        schema:
          type: string
